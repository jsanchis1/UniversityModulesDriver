language: java

jdk:
  - oraclejdk8
  
before_install:
   - rm -rf ~/.m2
   - mkdir -p ~/.m2
   
after_success:
   - mvn clean -U
   - mvn install

notifications:
    email:
        recipients:
            - j.sanchisarderius1@nuigalway.ie
